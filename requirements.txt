# ChargeGrid Assistant — Dependências
# Sprint 2 | EV Challenge 2026 | FIAP + GoodWe

# Ollama — cliente Python para o servidor (local ou nuvem)
ollama>=0.3.1

# Interface web
streamlit>=1.35.0

# Carregamento de variáveis de ambiente
python-dotenv>=1.0.0